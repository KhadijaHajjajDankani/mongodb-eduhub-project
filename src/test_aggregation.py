from aggregation_queries import (
    get_enrolled_students,
    count_students_per_instructor,
    average_grade_per_assignment,
    enrollment_stats
)

get_enrolled_students("C001")
count_students_per_instructor()
average_grade_per_assignment()
enrollment_stats()